import util.test as test


def x_plus_y_times_z(x, y, z):
    """
    Parameters: integers x, y, z
    Returns: the sum of the first two integers, multiplied by the third integer
    """
    # Start of your code
    return None
    # End of your code


test.test_equals(x_plus_y_times_z(1, 2, 3), 9)
test.test_equals(x_plus_y_times_z(-1, -1, 0), 0)
test.test_equals(x_plus_y_times_z(3, 4, -2), -14)
